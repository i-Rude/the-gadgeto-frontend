import GotoButton from "../components/GotoButton";

export default function CheckPage() {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1 className="text-3xl font-bold">Check Page</h1>
            <GotoButton />
        </div>
    );
}